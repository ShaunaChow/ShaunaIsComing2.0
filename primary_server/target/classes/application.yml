server:
  port: 28000
spring:
  application:
    name: Primary-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:28001/eureka/
  instance:
    instance-id: Primary-server
    prefer-ip-address: true